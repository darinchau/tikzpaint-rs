// use std::rc::Rc;

// use tikzpaint_rs::figures::{Coordinates, Matrix, Concat, Projection};

fn main() {
    // let x = Coordinates::new(&[3, 4, 5]);
    // let proj1 = Matrix::new([
    //     [1, 0, 0],
    //     [0, 1, 0],
    //     [0, 0, 2]
    // ]);
    // let proj2 = Rc::new(Matrix::new([
    //     [1, 0, 2],
    //     [2, -1, 1]
    // ])) as Rc<dyn Projection<3, 2>>;
    // let proj3 = Concat(Rc::clone(&proj1), Rc::clone(&proj2));
}